[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "skyline-say"
version = "0.1.0"
description = "Timed folder sentence tool"
requires-python = ">=3.9"

[project.scripts]
say = "say.cli:main"

[tool.setuptools]
packages = ["say"]
